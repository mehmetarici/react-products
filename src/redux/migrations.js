export const migrations = {
    0: (previousState) => ({
        productStore: previousState.productStore,
    }),
    1: (previousState) => ({
        productStore: previousState.productStore,
    }),
};
